# 工作室官网 · 本地预览

## 一键启动

```bash
cd studio-website
npm run dev
```

浏览器打开：**http://localhost:5180**

定价页：**http://localhost:5180/pricing**

## 若打不开

1. **不要用 5181** — 只有当前终端里 Vite 打印的地址才有效。
2. 关掉旧进程后重开：
   ```bash
   lsof -ti :5180 | xargs kill -9 2>/dev/null
   npm run dev
   ```
3. 仍失败时用构建预览（不依赖热更新）：
   ```bash
   npm run build && npm run preview
   ```

## 行业 Demo（另开终端）

| 项目 | 命令 | 地址 |
|------|------|------|
| 清洁 demo1 | `cd demo/demo1 && npm run dev` | http://localhost:5173 |
| 美容 demo2 | `cd demo/demo2 && npm run dev` | http://localhost:5174 |

首页「Open demo」链接依赖上述两个服务同时运行。
